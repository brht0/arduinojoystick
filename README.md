# Arduino C++ interface for a joystick 

- SFML for joystick events.
- Sends joystick events in a simple format through Serial USB
- Uses stty command to open communication with Arduino, may require sudo priviledges.
